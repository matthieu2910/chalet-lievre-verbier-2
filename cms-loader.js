fetch('/content/home.md').then(r=>r.text()).then(text=>{const data=parseFrontMatter(text);document.getElementById('title').innerText=data.title;document.getElementById('short_description').innerText=data.short_description;document.getElementById('description').innerHTML=data.description;document.getElementById('location').innerText=data.location;const c=document.getElementById('carousel');c.innerHTML=(data.carousel||[]).map(o=>`<img src="${o.image}" />`).join('');const a=document.getElementById('amenities');a.innerHTML=(data.amenities||[]).map(o=>`<li>${o.item}</li>`).join('')});function parseFrontMatter(text){const fm=text.split('---');const yaml=fm[1];const lines=yaml.split(/\r?\n/);const obj={};let key=null;lines.forEach(line=>{if(line.includes(':')&&!line.trim().startsWith('-')){const idx=line.indexOf(':');key=line.substring(0,idx).trim();obj[key]=line.substring(idx+1).trim()||[]}else if(line.trim().startsWith('-')){if(!Array.isArray(obj[key]))obj[key]=[];const val=line.replace('-','').trim();if(key==='carousel')obj[key].push({image:val});else obj[key].push({item:val})}});return obj}