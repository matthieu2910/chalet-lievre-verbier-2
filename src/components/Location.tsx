"use client";  import { motion } from "framer-motion"; import { useInView } from "framer-motion"; import { useRef } from "react"; import { MapPin, Mountain, Clock, Plane, Train, Car } from "lucide-react";  const distances = [   {     icon: Mountain,     name: "Savoleyres lift",     distance: "3 min walk",   },   {     icon: MapPin,     name: "Verbier Village Center",     distance: "5 min walk",   },   {     icon: Plane,     name: "Geneva Airport",     distance: "2h drive",   },   {     icon: Train,     name: "Le Châble Train Station",     distance: "10 min drive",   }, ];  const activities = [   {     season: "Winter",     items: [       "World-class skiing & snowboarding",       "Off-piste adventures",       "Ice climbing",       "après-ski",       "Spa & wellness",     ],   },   {     season: "Summer",     items: [       "Mountain biking",       "Hiking trails",       "Golf courses",       "Paragliding",       "Fine dining",     ],   }, ];  export function Location() {   const ref = useRef(null);   const isInView = useInView(ref, { once: true, margin: "-100px" });    return (     <section id="location" ref={ref} className="section-padding bg-alpine-900">       <div className="container-wide">         <div className="grid lg:grid-cols-2 gap-12 lg:gap-20">           {/* Left: Content */}           <motion.div             initial={{ opacity: 0, x: -30 }}             animate={isInView ? { opacity: 1, x: 0 } : {}}             transition={{ duration: 0.8 }}           >             <p className="text-gold-400 tracking-[0.2em] uppercase text-sm mb-4">               At the Heart of Verbier             </p>             <h2 className="font-serif text-4xl md:text-5xl lg:text-6xl text-white mb-8">               Prime <span className="italic font-light">Location</span>             </h2>             <p className="text-alpine-300 leading-relaxed mb-8">               Chalet d'Adrien enjoys a prime location in one of Verbier's most prestigious               neighborhoods. Just steps from the Savoleyres lift—with private access—and               the lively village center, you'll have immediate access to all the legendary               resort has to offer, without compromising on tranquility or privacy.             </p>              {/* Distances */}             <div className="grid grid-cols-2 gap-4 mb-12">               {distances.map((item, index) => (                 <motion.div                   key={item.name}                   initial={{ opacity: 0, y: 20 }}                   animate={isInView ? { opacity: 1, y: 0 } : {}}                   transition={{ duration: 0.5, delay: 0.2 + index * 0.1 }}                   className="flex items-start gap-3 p-4 bg-alpine-800/50 border border-alpine-700"                 >                   <item.icon className="w-5 h-5 text-gold-400 flex-shrink-0 mt-0.5" />                   <div>                     <p className="text-white text-sm font-medium">{item.name}</p>                     <p className="text-alpine-400 text-xs">{item.distance}</p>                   </div>                 </motion.div>               ))}             </div>              {/* Activities */}             <div className="grid grid-cols-2 gap-8">               {activities.map((activity) => (                 <div key={activity.season}>                   <h3 className="font-serif text-xl text-white mb-4">                     {activity.season}                   </h3>                   <ul className="space-y-2">                     {activity.items.map((item) => (                       <li                         key={item}                         className="text-alpine-300 text-sm flex items-center gap-2"                       >                         <span className="w-1 h-1 bg-gold-400 rounded-full" />                         {item}                       </li>                     ))}                   </ul>                 </div>               ))}             </div>           </motion.div>            {/* Right: Map Placeholder */}           <motion.div             initial={{ opacity: 0, x: 30 }}             animate={isInView ? { opacity: 1, x: 0 } : {}}             transition={{ duration: 0.8, delay: 0.2 }}             className="relative"           >             <div className="aspect-square lg:aspect-auto lg:h-full min-h-[400px] bg-alpine-800 border border-alpine-700 flex items-center justify-center">               {/* Stylized Map Placeholder */}               <div className="text-center p-8">                 <div className="w-16 h-16 mx-auto mb-6 rounded-full bg-gold-600/20 flex items-center justify-center">                   <MapPin className="w-8 h-8 text-gold-400" />                 </div>                 <h3 className="font-serif text-2xl text-white mb-2">                   Verbier, Switzerland                 </h3>                 <p className="text-alpine-400 text-sm mb-6">                   1936m altitude ÔÇó Valais Canton                 </p>                 <a                   href="https://maps.google.com/?q=Verbier,Switzerland"                   target="_blank"                   rel="noopener noreferrer"                   className="inline-flex items-center gap-2 px-6 py-3 bg-gold-600 text-white text-sm font-medium tracking-wider uppercase hover:bg-gold-500 transition-colors"                 >                   View on Google Maps                 </a>               </div>             </div>              {/* Decorative Border */}             <div className="absolute -top-4 -left-4 w-full h-full border border-gold-600/30 -z-10 hidden lg:block" />           </motion.div>         </div>       </div>     </section>   ); }